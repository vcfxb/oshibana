//! Utilities for saving the app's user state

use std::sync::{Arc, Mutex};
use std::sync::atomic::AtomicBool;
use crate::app::Oshibana;



impl Oshibana {
    async fn save(&self) {
        
    }
}
